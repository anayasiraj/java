package conwaygame;

public class nodeStuff {
    /* 
   private void insertAfter(Node head, int target, int newValue){
    
    Node a = new Node;
    a.data = newValue;
    Node pointer = head;

    while (head != target && head.next != null){
        head = head.next;
    }
        a.next = head.next;
        head.next = a;
    
   }

   private int max (Node head){
    if (head ==  null) return 0;
    return Math.max (head.data, max(head.next));
   } */
}
